# ---------------------------------------------------------------------------
# Reverse-engineering scratch data — NOT for publication
# ---------------------------------------------------------------------------
# Decompiled Moov Coach app and the APK itself are proprietary/copyrighted.
# Only the protocol findings documented in ble_tools/protocol_notes.md are
# ours to publish. Never commit these.
*.apk
.tmp_apk/
.tmp_moov_decompiled/
.tmp_moov_repo/
.tmp_*

# ---------------------------------------------------------------------------
# One-off reverse-engineering / diagnostic scripts (kept locally, not shipped)
# ---------------------------------------------------------------------------
ble_tools/explorer.py
ble_tools/auto_connect.py
ble_tools/check_moov_signal.py
ble_tools/identify_moov.py
ble_tools/moov_latch.py
ble_tools/moov_sniffer.py
ble_tools/probe_candidates.py
run_explorer.py
run_connect.py
run_check.py
run_identify.py
run_latch.py
run_sniffer.py
run_probe.py

# ---------------------------------------------------------------------------
# Local runtime data
# ---------------------------------------------------------------------------
*.db
*.sqlite
*.sqlite3
graphify-out/
image.png
plan.txt
# Misnamed dump: this is an iPhone capture, not the Moov (contains personal device name)
gatt_dump.json

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# ---------------------------------------------------------------------------
# Android / Flutter
# ---------------------------------------------------------------------------
# Signing material — supplied via CI secrets, never committed
android_app/android/key.properties
android_app/android/app/*.jks
android_app/android/app/*.keystore
*.jks
*.keystore
key.properties

# Build output
android_app/build/
android_app/.dart_tool/
android_app/.flutter-plugins
android_app/.flutter-plugins-dependencies
android_app/android/.gradle/
android_app/android/local.properties
android_app/android/app/debug/
android_app/android/app/profile/
android_app/android/app/release/
android_app/**/*.iml
.gradle/

# ---------------------------------------------------------------------------
# Secrets — never commit
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.b64
service-account*.json
*service*account*.json

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
